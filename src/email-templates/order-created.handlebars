<h2 style="margin:0 0 20px;font-size:20px;text-align:center;">Your order has been created!</h2>
<p style="margin:0 0 28px;text-align:center;">Thank you for shopping with DripNat. Here's a quick summary of what you just ordered 👇</p>

<!-- PRODUCT LIST -->
<div style="border:2px solid #000;padding:12px;">
  {{#each products}}
    <div style="display:flex;align-items:center;padding:12px 0;{{#unless @last}}border-bottom:1px solid #000;{{/unless}}">
      <!-- product image (fallback square) -->
      <div style="flex:0 0 72px;height:72px;border:2px solid #000;overflow:hidden;margin-right:16px;">
        {{#if cid}}
          <img src="cid:{{cid}}" alt="{{title}}" style="width:100%;height:100%;object-fit:cover;display:block;border:0;" />
        {{else}}
          <div style="width:100%;height:100%;background:#f5f5f5;display:flex;align-items:center;justify-content:center;font-size:12px;">No&nbsp;Img</div>
        {{/if}}
      </div>

      <!-- product details -->
      <div style="flex:1 1 auto;">
        <p style="margin:0 0 4px;font-weight:bold;">{{title}}</p>
        <p style="margin:0 0 4px;font-size:13px;">SKU: {{skuId}}</p>
        <p style="margin:0;font-size:13px;">Qty: {{quantity}}</p>
      </div>

      <!-- price -->
      <div style="flex:0 0 80px;text-align:right;font-weight:bold;">${{price}}</div>
    </div>
  {{/each}}
</div>

<!-- TOTAL -->
<p style="margin:24px 0 0;text-align:right;font-size:16px;font-weight:bold;">Total:&nbsp;${{total}}</p>

<p style="margin-top:28px;text-align:center;">We'll notify you when your order status changes.</p> 